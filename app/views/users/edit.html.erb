<div class="container">
	<div class="row">
		<div class="col-xs-9 col-xs-offset-1">
			<h3>登録情報編集</h3>
			<ul>
				<%= form_for(@user) do |f| %>
				<li>
					<label class="warning">ユーザーネーム</label>
						<%= f.text_field :name, autofocus: true, autocomplete: "name" %>
				</li>

				<li>
					<div class="user-edit-image">
						<p>現在のプロフィール画像</p>
				        <%= attachment_image_tag @user, :user_image, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg", size: "60x60"%>
				    </div>
				</li>
				<li>
				    <div>
					<%= f.attachment_field :user_image %>
					</div>
				</li>
				<li>
					<label class="warning">ユーザー詳細</label>
					<%= f.text_area :introduction, autofocus: true, autocomplete: "introduction" %>
				</li>
				<li>
					<label class="warning">メールアドレス</label>
					<%= f.email_field :email, autofocus: true, autocomplete: "email",:size => "50*30" %>
				</li>
					<%= f.submit "編集内容を保存する", class: "btn btn-info" %>
					<% end %>
			</ul>
		</div>
	</div>
</div>